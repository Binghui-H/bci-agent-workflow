# TODO

## 可配置领域层

当前版本是垂直 BCI 工作流，角色默认加载 `bci-domain`，覆盖 EEG、EMG、EOG、IMU 和相关生理信号算法约束。

后续将领域规范改造成用户可追加的领域包，同时保持编排、设计、执行、审阅和验证流程不变。

| 事项 | 目标 |
|---|---|
| 领域配置 | 在工作流配置中声明一个或多个领域 skills，不把 `bci-domain` 写死在角色流程中 |
| 用户扩展 | 允许用户提供自己的领域 skill，并为不同项目选择或组合领域规范 |
| 默认兼容 | 保留 BCI 领域包作为默认垂直实现，不影响现有项目 |
| 角色加载 | 设计、执行、审阅和验证角色按配置加载同一组领域 skills |
| 契约扩展 | 允许领域包追加设计章节、门禁条件、测试指标和安全边界 |
| 冲突处理 | 定义多个领域包之间的优先级、冲突报告和阻塞规则 |
| 初始化支持 | 初始化脚本根据配置生成领域清单，并检查所需 skills 是否已经安装 |
| 文档与验证 | 增加自定义领域包模板、示例和端到端验证用例 |

在该能力完成前，用户仍可直接扩展 `bci-domain`，或创建自己的领域 skill 并在启动提示中要求对应角色加载；这种方式属于人工约定，尚未由状态机自动校验。
